name: ridgesandvalleys
type: wordpress
docroot: web
php_version: "8.3"
webserver_type: nginx-fpm
database:
  type: mariadb
  version: "10.11"
composer_version: "2"
web_environment:
  - WP_ENV=development
hooks:
  post-start:
    - exec-host: "test -f .env || cp .env.example .env"
