{
	"$schema": "https://schemas.wp.org/trunk/wp-env.json",
	"core": null,
	"phpVersion": "8.3",
	"themes": ["."],
	"config": {
		"WP_DEBUG": true,
		"WP_DEBUG_LOG": true,
		"WP_DEBUG_DISPLAY": false,
		"WP_MEMORY_LIMIT": "256M"
	},
	"lifecycleScripts": {
		"afterStart": "wp-env run cli wp theme activate ridgesandvalleys-theme && wp-env run cli wp rewrite structure '/%postname%/' --hard && wp-env run cli wp rewrite flush --hard"
	}
}
