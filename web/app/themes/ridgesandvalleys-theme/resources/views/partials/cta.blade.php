{{-- Retired 2026-08-06: the standalone orange "Ready to be easy to find?" CTA band
     was removed sitewide. The footer newsletter now serves as the single closing CTA.
     Intentionally renders nothing; kept so any stray @include('partials.cta') is a no-op. --}}
