name: Deploy theme to SiteGround

# Builds the Sage theme and rsyncs it to the live theme folder over SSH.
# Deploys CODE ONLY -- never the database or uploads. Content is never touched.
on:
  push:
    branches: [main]
    paths:
      - "web/app/themes/ridgesandvalleys-theme/**"
      - ".github/workflows/deploy-theme.yml"
  workflow_dispatch:

concurrency:
  group: deploy-production
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.3"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: web/app/themes/ridgesandvalleys-theme/package-lock.json

      - name: Build theme (Acorn + Vite)
        working-directory: web/app/themes/ridgesandvalleys-theme
        run: |
          composer install --no-dev --prefer-dist --no-interaction --optimize-autoloader
          npm ci
          npm run build

      - name: Prepare SSH
        run: |
          mkdir -p ~/.ssh
          printf '%s\n' "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/deploy_key
          chmod 600 ~/.ssh/deploy_key
          ssh-keyscan -p "${{ secrets.SSH_PORT }}" "${{ secrets.SSH_HOST }}" >> ~/.ssh/known_hosts 2>/dev/null

      - name: Deploy theme via rsync
        run: |
          rsync -az --delete \
            --exclude ".git" \
            --exclude "node_modules" \
            --exclude ".dev-wp" \
            --exclude "dev" \
            --exclude "concept" \
            --exclude "docs" \
            --exclude "resources/css" \
            --exclude "resources/js" \
            --exclude "package.json" \
            --exclude "package-lock.json" \
            --exclude "vite.config.js" \
            --exclude "tsconfig.json" \
            --exclude "eslint.config.js" \
            --exclude ".wp-env.json" \
            -e "ssh -i ~/.ssh/deploy_key -p ${{ secrets.SSH_PORT }}" \
            web/app/themes/ridgesandvalleys-theme/ \
            "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}:${{ secrets.DEPLOY_PATH }}/"

      - name: Flush WordPress cache
        run: |
          ssh -i ~/.ssh/deploy_key -p "${{ secrets.SSH_PORT }}" \
            "${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }}" \
            "wp --path=~/www/ridgesandvalleys.com/public_html cache flush" || true
