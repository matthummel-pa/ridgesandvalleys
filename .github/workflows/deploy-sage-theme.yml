name: Deploy Sage theme (SiteGround)

# Diagnostic: previous copies of this workflow never allocated a job
# (startup_failure). Prove the file can start, then restore build+rsync.
on:
  push:
    branches: [main]
    paths:
      - "web/app/themes/ridgesandvalleys-theme/**"
      - ".github/workflows/deploy-sage-theme.yml"
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: echo "deploy workflow started"
